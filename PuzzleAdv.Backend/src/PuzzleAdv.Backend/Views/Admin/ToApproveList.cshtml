@model IEnumerable<PuzzleAdv.Backend.ViewModels.Admin.AdminListViewModel>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Puzzle da approvare";
}

<div class="wrapper wrapper-content animated fadeInRight">
    <div class="container">
        <div class="ibox">
            <div class="ibox-title">
                <h4>Puzzle da approvare</h4>
            </div>
            <div class="ibox-content">
                <div class="row">

                    @foreach (var item in Model)
                {

                        <div class="col-md-3">
                            <div class="ibox">
                                <div class="ibox-content product-box">
                                    <div class="ibox-content no-padding border-left-right">
                                        <img alt="image" class="img-responsive" src="~/Uploads/16-9/400/@(item.PuzzleImage).jpg">
                                    </div>
                                    <div class="product-desc">
                                        <small class="text-muted">#@(item.IDNumber) | @Html.DisplayFor(modelItem => item.InsertDate)</small>
                                        <a href="#" class="product-name">
                                            @Html.Raw(item.ShopTitle) @Html.Raw(item.ShopCity)
                                        </a>
                                        <div class="m-t text-left">
                                            <a asp-action="ApprovePuzzle" asp-route-id="@item.ID" class="btn btn-xs btn-outline btn-primary">
                                                Gestisci <i class="fa fa-long-arrow-right"></i>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                }

                </div>
            </div>
        </div>
    </div>
</div>